# Agent 模型预算与体验边界

更新时间：2026-09-07

## 1. 模型预算

当前预算解析参考本地 Codex CLI 源码快照 `4b0d9669cc` 的 `models-manager`：模型目录、远端元数据和内部预算策略分离，不根据模型是否属于某一个名字决定窗口大小。常规聊天完全由 main 自动解析预算，用户只选择模型和推理强度。目录已统一到 `agent-model-profiles.ts`，模型提示词与工具协议的实际适配见 [模型执行适配](agent-model-adaptation.md)。

OmniFlow 的解析顺序：

1. 从当前配置连接的 `/models` 读取元数据，支持 `data / models` 集合、`id / slug / name`、`context_window / context_length`、`max_context_window`、`max_output_tokens` 与 `top_provider.max_completion_tokens`。
2. 没有远端窗口时，OpenAI 类型使用随应用发布的精简 Codex 模型目录，支持精确 ID 和明确日期后缀，不用宽泛前缀猜测任意别名。
3. 未知模型使用 `16,384` 上下文和 `4,096` 输出预留。普通 OpenAI-compatible `/models` 可能只有 ID，不承诺一定能够自动发现窗口；缺少信息时应完善供应商元数据或维护内置目录，不要求用户在聊天中填写 token 数值。

目录数值是客户端默认值，不等于供应商当前模型规格。例如该 Codex 快照中 `gpt-5.4` 默认窗口为 `272,000`，最大可配置窗口为 `1,000,000`；不能把最大窗口自动当成默认窗口。远端元数据优先于本地目录。维护目录时核对来源快照，不随意推测新增模型规格。

main 中的模型目录缓存按 Provider、Base URL 和凭据的不可逆摘要隔离，最多保留 32 个连接；同连接合并并发查询。请求超时 3 秒，成功缓存 5 分钟，失败冷却 30 秒并使用已有缓存或目录回退。凭据和完整上游响应不进入 renderer 或 Agent transcript。

Composer 不提供预算编辑按钮或弹层，相关 `agent:model:budget` IPC 和 preload 方法已移除。`agent:chat:start` 不再声明或消费 `modelBudget`；旧客户端即使发送该遗留字段，main 也只按当前连接的元数据和模型目录自动解析，不接受隐藏的 renderer 覆盖。每个 Run 在启动前解析并冻结预算，之后沿用原有压缩与 usage 低估补偿机制。

读取模型偏好时只清理已退役的 `agent-model-budget:v1:*` 键，保留模型、推理强度和其他应用设置。即使本地存储不可清理，旧值也没有生效路径。底层 resolver 保留有界配置注入和校验能力，供内部测试或后续供应商高级配置接入；当前没有面向用户的高级预算配置入口。

有效请求窗口保留 5% 余量，输出上限自动遵循服务端限制、本地最多 `16,000` tokens 和不超过窗口四分之一的预留策略；可见文本另受每 turn / Run 的 `64,000` 字符保护。更大的模型理论输出能力不等于客户端已经允许无限累积文本。main-owned ledger 和 Run 快照继续记录服务端基准、增量估算及安全可追加空间，不把累计 usage 当作窗口占用；Composer 暂不展示，最终单数字呈现留待 UI 收尾，详情见 [上下文计量](agent-context-accounting.md)。

## 2. 流式可靠性

- OpenAI-compatible 必须有合法 `finish_reason`；Claude 必须同时取得合法 `stop_reason` 与 `message_stop`。单纯 EOF 不代表成功。
- `length / max_tokens`、内容过滤、异常结束或不完整响应明确失败并保留已显示内容，不将半截回答或工具参数当作完成。
- 未声明 Tool 的兼容请求也走相同的校验链路，省略 `tools` 字段，不能通过纯文本 fallback 绕过校验。
- 120 秒无响应活动触发空闲超时。读取到终止标记后释放流，不继续等待网关关闭连接。
- 对网络错误、408 / 429 / 500 / 502 / 503 / 504、空闲超时和不完整流，只有本次尚未向用户显示文本时才至多重试两次；等待退避可取消。不自动重放 Tool、已显示的回答、上下文超限或输出超限。
- 历史摘要仍是独立的无 Tool 调用；摘要失败沿用原有有界回退，不影响规范 ToolRun 事实。

## 3. 感知与界面

- 目录感知最多保留 200 项，但 `entryCount` 保留规范化后的总数，`truncated` 明确标识不完整；main 再次规范化时保留此信息。
- `file.list` 返回总数、实际返回数和截断标记，不能以部分结果断言未展示文件不存在。当前仍不支持自主递归浏览和目录续页 Tool，不能把本次修复写成全库检索能力。
- 失败、取消和中断 Run 在时间线保留规范终态行；即使 Provider 在首个文字输出前失败，重开会话后仍显示原因。空 streaming item 显示“正在思考”，不创建假消息或另一套 Run 状态。
- Composer 支持运行中起草；IME composition 与兼容 keyCode 229 的回车不提交，Shift+Enter 保持换行。停止点击必须阻止表单默认动作，避免快速终态把同次点击变成草稿提交；停止失败显示错误并保留现场。任务仍在运行时不发送新草稿，不提供自动排队或运行中插入指令。
- 最终回答使用 `react-markdown + remark-gfm` 展示标题、列表、表格和代码；禁用 HTML、MDX、外部媒体加载和模型链接导航。链接只保留文本，图片只保留 alt，代码和完整回答可复制。过程说明仍为纯文本。
- 当前不增加模型生成的可执行 UI、本地路径打开、产物预览或任意导航入口。持续进程会话、工具并行、运行中插入指令、附件与受控产物打开需独立升级协议。

## 4. 验证

自动化覆盖模型目录回退、日期 ID、远端元数据覆盖、内部配置校验、连接缓存隔离、旧偏好清理、忽略遗留 renderer 预算参数、流提前结束、输出上限、Claude 终态、临时错误重试、超时与取消、201 项目录、空失败时间线、IME、草稿保留，以及 Markdown 内容安全和复制。

常规门禁为 `npm run lint`、`npm test`、`npm run build`。界面验证使用隔离模拟会话检查亮暗主题、桌面窄宽窗口、代码与表格滚动、无预算编辑入口、模型选择、输入和复制。真实 Provider、目录递归、真实资料库写入、macOS 原生输入法和安装包行为不能由模拟 UI 验证替代。任何真实资料库验证禁止使用第一个资料库。

2026-09-07 预算入口退役后已通过 lint、完整构建和全量测试（1,891 项通过、7 项跳过）。Playwright 使用真实 AgentWorkspace 与隔离模拟 IPC，在 `1120 x 720` 亮色和 `1440 x 900` 暗色下确认无预算控件、旧偏好被清理且其他设置保留、发送请求不包含 `modelBudget`，同时回归代码复制、失败恢复、合成 IME、运行中草稿与快速停止。无页面异常、横向溢出、真实后端请求或外部图片加载；未使用真实模型、资料库或凭据。
