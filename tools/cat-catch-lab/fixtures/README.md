# Fixtures

本目录只在真实行为场景落地时新增 `<fixture-id>/`。`docs/cat-catch/capability-map.json` 中的计划名称不是已完成 fixture。

每个目录至少包含 `fixture.json`、输入和 expected；metadata 的 `testRefs` 必须指向实际测试文件。禁止提交只有名称和空 expected 的占位 fixture。
